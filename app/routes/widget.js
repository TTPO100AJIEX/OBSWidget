async function register(app, options)
{
    // TODO: /session/<%=session.id%>/widget
}

import plugin from 'fastify-plugin';
export default plugin(register, { name: 'oauth-routes', encapsulate: false });